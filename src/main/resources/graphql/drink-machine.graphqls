schema {
    query: Query
}

type Query {
    drinks(pagination:Pagination, sort:[Sort]): Drinks
}

type Drinks {
    values: [Drink]!,
    result: Result!
}

type Drink {
    name: String,
    amount: Int,
    price: Float
}

input Pagination {
    limit: Int,
    offset: Int
}

input Sort {
    direction:SortDirection,
    property:String!
}

enum SortDirection {
    ASC, DESC
}

type Result {
    totalCount: Int,
    count:Int,
    offset: Int
}